# Efforts index

One line per effort under `.scratch/`, so a fresh session can see what exists without opening any map body. See `docs/agents/issue-tracker.md` for tracker conventions.

- [Tartarus keybinder](tartarus-keybinder/map.md) — **archived**, MVP shipped. Daemon+GUI keybinding/macro control for the Razer Tartarus Pro (Profiles, Layers, Fire-once/Hold-to-repeat/Toggle, D-Bus, systemd packaging).
- [Tartarus input expansion](tartarus-input-expansion/map.md) — **archived**, v1.0 reached. Charted the course from the shipped MVP to a feature-complete v1.0 (Chord/Stepper/Profile-Switch/Controller-button/Axis Actions, the Macro & Stepper libraries, grid-key analog capture + Analog-repeat, the full output picker, tray icon, About dialog, release docs) and executed it ticket by ticket. Final ticket (107) settled the public-repo layout: development continues on the `dev` branch, `main` is a clean release-only branch. Post-1.0 refinements (Analog-repeat/axis rate curves, joystick hat direction) go to a new effort.
- [Tartarus status-LED indicator](tartarus-status-leds/map.md) — **spec ready** (wayfinder, all tickets resolved 2026-09-02). Charted the way to a gated [`spec.md`](tartarus-status-leds/spec.md) for a feature that lets each Profile define an on/off state for the three side Status LEDs (orange/green/blue), asserted on Profile switch, on Daemon startup, and on every device (re)connect. **Kill-gate PASSED** (ticket 01, fw v1.2): all three LEDs independently host-controllable via the `0x0F/0x02` LED-ID-`0x0B` static-effect frame, all-off reachable, no adverse device behaviour, every byte of ticket 02's source-derived wire spec verified on hardware. Tickets 01–05 resolved; [`spec.md`](tartarus-status-leds/spec.md) + [ADR-0006](../docs/adr/0006-status-leds-driven-from-dispatch-over-short-lived-hidraw-fds.md) + the `Status LED` / `Status LED assignment` `CONTEXT.md` entries are written. **Implementation is a fresh effort** — this map is done and can be archived. Key decisions in the spec: stateless `assert_status_leds()` / `clear_status_leds()` in `analog.rs` (short-lived Interface-2 hidraw fd, no driver mode), driven by a dedicated non-fatal `led` task fed a `watch<Option<StatusLeds>>` from dispatch (the sole `Config` owner); `Effect::AssertStatusLeds` on Profile switch + set-edit, assert on every connect edge, all-off on clean exit; `StatusLeds { orange, green, blue }` struct + `#[serde(default)]` `Profile.status_leds` (no `schema_version` bump — all-off serde default is the migration); `Edit::SetStatusLeds` / `SetStatusLeds(bbb)` whole-triple; no `GetState()` addition, no new signal; GUI "Status LEDs" group (three colour lozenges, lit/unlit) in `device_row` between thumbstick and Chords per the user's mockup; nothing in `rules.py`. Research files `tartarus-pro-status-leds.md` (grounding) + `status-led-wire-protocol.md` (ticket 02); prototype `prototype/01-status-leds/`.
- [Tartarus status-LED indicator — implementation](tartarus-status-leds-impl/issues/) — **all four tickets done** (2026-09-02), non-wayfinder (ticket files only, no map). The fresh implementation effort handed off by [`tartarus-status-leds/spec.md`](tartarus-status-leds/spec.md) (charting done, kill-gate passed). Four tickets in a near-linear chain: (01) per-Profile `StatusLeds { orange, green, blue }` config struct + `#[serde(default)]` field + `profile_to_dict` wire entry (no `schema_version` bump); (02) `assert_status_leds` / `clear_status_leds` in `capture/analog` + a non-fatal `led` task fed a `watch<Option<StatusLeds>>`, asserted on every `connected == true` and cleared on clean exit; (03) `Edit::SetStatusLeds` / `SetStatusLeds(bbb)` + unconditional `Effect::AssertStatusLeds` on set-edit and Profile switch + the `daemon_client`/`daemon_stub` mirror; (04) the "Status LEDs" three-lozenge group in the Device Overview `device_row`. ADR-0006 and the `Status LED` / `Status LED assignment` `CONTEXT.md` entries were written with the spec.
- [Tartarus dual-stage grid keys](tartarus-dual-stage-keys/map.md) — **spec ready** (wayfinder, all six tickets resolved 2026-09-04). Charted the way to a gated [`spec.md`](tartarus-dual-stage-keys/spec.md) for "Dual-stage keys": a grid key in analog Capture mode may carry a second **Actuation stage** (its own Actuation/Release pair + `Binding`) firing at a Depth strictly deeper than the primary stage, with a user-selectable **Staging mode** (Handoff / No-Return / Additive / Quick-Skip) governing the handoff. No kill-gate (all software). Tickets 01–06 resolved; [`spec.md`](tartarus-dual-stage-keys/spec.md) + [ADR-0007](../docs/adr/0007-dual-stage-depth-interpretation-in-dispatch.md) + the `Actuation stage` / `Staging mode` / `Handoff` / `No-Return` / `Additive` / `Quick-Skip` `CONTEXT.md` entries are written. **Implementation is a fresh effort** — this map is done and can be archived. Key decisions in the spec: capture source unchanged, all staged-depth interpretation in a new pure `daemon/src/stage.rs` + non-pure `stage::Engine` (third depth engine alongside `axis`/`analog_repeat`) holding a `trigger::Slots<StageKey>`; cap at 2 stages; deep stage requires a primary, cascade-deletes with it; disjoint stacked bands (`deep.release > primary.actuation`); deep Binding per-Layer, deep ActuationPoint + mode per-Input per-Profile; four Staging-mode event-sequence tables (mechanical replay, no fast-ramp short-circuiting); Analog-repeat banned on dual-stage keys; Chord member ⇒ no deep stage; deep stage inert in Digital mode; seven new `ConfigError` variants + four granular `Edit` variants; GUI binding editor is one flat panel with a Primary/Deep swap toggle so only one key/button picker is ever mounted at once (prototype: `prototype/04-dual-stage-binding-editor-layout/`); Layer/Profile switch and the Digital-mode flip force-release both stages (`Effect::StopAllStages`), cascade-delete force-releases immediately (`Effect::StopStage`), reconnect gets a new explicit disconnect hook; no new `GetState()` field. Analog-repeat's pre-existing lack of dropout handling, surfaced while resolving the reconnect question, is explicitly out of scope as a separate effort.
- [Tartarus dual-stage grid keys — implementation](tartarus-dual-stage-keys-impl/issues/) — **active** (tickets 01–12 resolved; the binding-editor UX pass from Charon's grilling 2026-09-05 is complete, plus post-ship follow-ups 08/10/12 from testing), non-wayfinder (ticket files only, no map). The fresh implementation effort handed off by [`tartarus-dual-stage-keys/spec.md`](tartarus-dual-stage-keys/spec.md). Ticket 08 is a post-ship follow-up from Charon's testing — a Profile-remembered deep-band seed for `+ Add deep stage`, `Profile.default_deep_actuation`. Tickets 09–11 unify the grid-key binding editor. Ticket 09 (**resolved** 2026-09-06) collapsed the two layouts an unbound vs. bound grid key used to show into one: every non-Axis grid key on a dual-stage-aware Daemon opens the same swap-toggle panel, an unbound key rendering a synthetic primary stage (Keypress / `KEY_A` / the Input's default Trigger mode) with `+ Add deep stage` / `Clear Binding` disabled until a primary is committed; `build_dual_stage_panel` now reads its primary Binding from the config snapshot rather than a constructor argument (so ticket 10's in-place rebuild reflects a fresh commit), the old plain-editor + boxed-Actuation layout is kept only as the `deep_base`-absent version-skew fallback, and the scroller chrome allowance dropped 280→96px. Ticket 10 (**resolved** 2026-09-06) added a plain **Apply** button to the grid-key panel that commits both stages exactly as Save does (shared `commit_stages()` seam, snapshot mutated in place) but rebuilds the panel locally instead of closing — so the unbound→bound transition and adding a deep stage both happen in one window session; `on_change()` moved off the per-Save path onto the editor window's `close-request` handler, fired once per session behind a `committed` flag set by the first Save/Apply push. Ticket 11 (**resolved** 2026-09-06) right-aligns the button rows consistently across the three editors — grid `[Clear Binding] [Apply] [Save]`, non-grid `[Clear Binding] [Save]`, Chord `[Cancel] [Save Chord]`, each `halign=END` with the primary commit in the corner; cosmetic-only, GUI-only. Ticket 11 also picked up a follow-up (unbound-key Key picker now seeds from the Input's own passthrough default — `inputs.default_key_code_for` / `INPUT_DEFAULT_KEY_CODE` — instead of a fixed `KEY_A`). Ticket 12 (**resolved** 2026-09-06, post-ship follow-up from Charon's testing) makes the grid-key editor's **Reset to Profile default** also return the deep band (re-seed from `default_deep_cfg()` and push `set_deep_actuation`) when the key has a deep stage; behaviour-only, GUI-only, no Daemon change. Original seven tickets: (01) `DeepStageConfig`/`StagingMode` config schema + seven `ConfigError` variants + `GetConfig` wire surface, no runtime/D-Bus-mutation yet — **resolved**; (02) the pure `stage.rs` state machine (`advance`/`StageOp`, the four Staging-mode transition tables, Quick-Skip's Armed/Skipped/Late) — **resolved**; (03) `stage::Engine` wired into dispatch as the third depth engine, live end-to-end for Handoff/No-Return/Additive — **resolved**; (04) Quick-Skip's dispatch-side primary-suppression buffer (`begin_quick_skip`/`is_late`/`next_deadline`/`tick` + the `handle_event` divert + `wait_for_stage_deadline`), completing all four modes — **resolved**; (05) the four `Edit`/D-Bus variants (`SetDeepStage`/`ClearDeepStage`/`SetDeepActuation`/`SetStagingMode`) + `daemon_client`/`daemon_stub`/`wire.py` mirror, independent of 02–04 — **resolved**; (06) runtime teardown — `Effect::StopAllStages` (Profile switch), `Effect::StopStage` (cascade-delete, wired into `SetBinding`/`ClearBinding` and mirrored in `daemon_stub.py`), the new disconnect hook (`handle_connection_change`'s `connected == false` branch), GUI-focus `StopAllToggles` draining `Slots<StageKey>` too — **resolved**; (07) `binding_editor.build_dual_stage_panel` — the real "swap toggle" panel (shared 4-marker `DepthTrack` bar — left `hexpand`; the spec's fixed-width bar was tried and reverted because it clipped the deep markers on a narrow window, see ticket 07's Answer — Primary/Deep toggle owning the sole editor slot, staging-mode row, `+ Add deep stage`/`✕`, Digital-mode greying, bind-primary-first gate on an unbound key — that gate later removed by ticket 09) plus the README "Dual-stage keys" section — **resolved**. Chain: 01→02→03→04, 01→05, {03,04}→06, {05,06}→07 (05 can run in parallel with 02–04 once 01 lands).
- [Humane output rate](humane-output-rate/map.md) — **archived 2026-09-07** (wayfinder, charted 2026-09-06, all thirteen tickets resolved). Archived once, reopened 2026-09-07 for two grilling tickets (10 & 11), re-archived when their fixes (tickets 12 & 13) landed. Ticket 10, [Fire-once keyboard keystroke dwell](humane-output-rate/issues/10-fire-once-keyboard-keystroke-dwell.md) — a single Fire-once keyboard press emits the same ~0 ms-dwell `[KeyDown, KeyUp]` the audit flagged as its clearest synthetic tell but was never in the "holds or repeats" scope — **resolved 2026-09-07**: yes, floor a canned one-shot keyboard press with a fixed 40 ms Down→Up dwell (scope = wherever `executor::single_held_key` matches, under Fire-once: plain + modifier Keypress, single-key Macro/Stepper-step/Chord; multi-step Macro, Analog-repeat, ControllerButton excluded by that predicate), framed as compliance not defense; execution graduated as ticket 12, [Splice the Fire-once keypress dwell](humane-output-rate/issues/12-splice-the-fire-once-keypress-dwell.md) (**resolved 2026-09-07** — `executor::FIRE_ONCE_KEY_DWELL` (40 ms) + `fire_once_key_steps`; mechanism = `perform`-local branch not the decide-split, since the split can't reach a Fire-once Stepper `Key` step; `fire_once_key_dwell: bool` on `PerformDeps` keeps the dwell off the `stage::Engine` re-press/deep paths; ADR-0008 + `CONTEXT.md` ceiling term amended, no new ADR; daemon 525 / GUI 503 green). Ticket 11, [Additive Staging mode — fix or cut](humane-output-rate/issues/11-additive-staging-mode-fix-or-cut.md) — the `value=2` rebuild (ticket 07) left the Dual-stage Additive mode emitting two concurrent kernel-autorepeat streams from one finger press. **Resolved 2026-09-07: cut the mode** — two held autorepeating keys is a shape no physical keyboard produces (the kernel autorepeats only the most-recently-pressed key), so it is an ADR-0008 violation; no use case, always the shakiest of the four modes; after the cut no staging mode emits two concurrent autorepeat streams. Execution graduated as [ticket 13](humane-output-rate/issues/13-remove-additive-staging-mode.md), [Remove the Additive staging mode](humane-output-rate/issues/13-remove-additive-staging-mode.md) (**resolved 2026-09-07** on `dev` — `StagingMode` → Handoff/No-Return/Quick-Skip; migration = a raw-TOML `find_removed_additive_staging` scan in `config::parse` → `ConfigError::RemovedStagingModeAdditive` (no retained serde marker variant); `stage.rs` `additive()`/table/arm deleted, Additive-naming self-references reworded; new **ADR-0009**, ADR-0007/0008 pointers, `CONTEXT.md`/README edits; daemon 525 / GUI 504 green, `grep -i additive` clean). Verifies every non-macro Action that holds or repeats synthetic output against one bar — Acheron never emits faster than the Linux input stack does for a physically held key/button (kernel autorepeat delay/period; held buttons one Down/Up) — fixes any surface that fails **within this map** (execution in scope), then feeds the finding into two gated specs (`spec-user-facing-output-safety-guidance.md` for the user-facing output-safety guidance; `spec-kernel-shaped-repeat.md` for `value=2`), plus an ADR + `CONTEXT.md` term ("Physical-plausibility ceiling"; "Humane output rate" is only this effort's working name). Tickets: (01) audit all holding/repeating output paths → 12-surface verdict table [**resolved** 2026-09-06, ratified with Charon — 8 OK, surfaces 2 & 5 flagged → ticket 06, cross-cutting "always `[KeyDown,KeyUp]` never `value=2`" → ticket 07, macro paths → ticket 03, pathological `kbdrate` ruled out of scope]; (02) `/research` on anti-cheat input-timing heuristics [**resolved** 2026-09-06 — `docs/anti-cheat-input-heuristics.md` (relocated from `research/anticheat-input-timing-heuristics.md` at ticket 05); regularity not raw rate is the universal tell, Linux held-key autorepeat is 250ms/33ms software repeat, `BTN_*` never repeats]; (03) decide the self-DoS macro guardrail [**resolved** 2026-09-06 — trigger-driven Macro *repetition* is already floored by existing mechanism (`run_toggle_loop` `target_lap` + the `FiringUnfinished` overlap guard), no new floor → test-only ticket 08; within-run burst + once-fired Macro stay unrestricted (text-only guidance); single-key Macro routes through ticket 07's `value=2` path; Analog-repeat + Macro banned → ticket 09]; (04) ADR + glossary term [**resolved** 2026-09-06 — `docs/adr/0008-physical-plausibility-ceiling-for-synthetic-output.md` (the ceiling, the audit + clamp, the `value=2` decision marked gated, the Macro exception, the "pathological `kbdrate` not guarded" note; first record of "uinput origin always detectable and accepted", NOT ADR-0002); `CONTEXT.md` term **"Physical-plausibility ceiling"** (renamed from "Humane output rate" per Charon)]; (05) spec the user-facing output-safety guidance (macro disclaimer + tips + Analog-repeat notice) [**resolved** 2026-09-06 — gated `spec-user-facing-output-safety-guidance.md`; two **GUI hints** not a toast (new CONTEXT.md `### Interface` terms Toast label vs GUI hint), `⚠️` macro-editor disclaimer + `⚠️` Analog-repeat notice, "About macro safety" `Gtk.Expander`, new README `## Output safety` section (no "safe" numbers), friendly no-warranty restatement; research doc moved to `docs/anti-cheat-input-heuristics.md`; text+placement only, no config check]; (06) clamp missed deadlines in both repeat pace loops [**resolved** 2026-09-06 — `RepeatSchedule::advance_fired` + `analog_repeat::tap_pace_wait`, two pure table-tested helpers]; (07) spec the kernel-shaped `value=2` repeat [**resolved** 2026-09-06 — gated `spec-kernel-shaped-repeat.md`; fork = inject `value=2` ourselves (evdev 0.13.2 can't enable `EV_REP` on a `VirtualDevice`); converts Digital + Analog-synth Hold-to-repeat, Chord single-key, Analog-repeat hold-solid, Toggle → keyboard Keypress / single-key Macro; NOT Stepper, button Toggles, or multi-step Macro loops; single-key predicate + `hold_repeat_kind` in `trigger`]; **implemented** in [`kernel-shaped-repeat-impl/`](kernel-shaped-repeat-impl/issues/) (7 tickets, all done 2026-09-06 — ADR-0008 un-gated, `CONTEXT.md` "Toggle" entry restructured); (08) lock the Macro-repetition floor with regression tests [**resolved** 2026-09-06 — test-only; the Toggle→Macro split landed with `kernel-shaped-repeat-impl` ticket 05, the Hold-to-repeat→Macro overlap-guard case already covered by `overlapping_same_input_firings_are_dropped_not_queued`]; (09) disallow the Analog-repeat + Macro Binding [**resolved** 2026-09-07 — execution ticket; new `ConfigError::AnalogRepeatMacro` in `config::binding::check_binding` enforced at `SetBinding` + `load_or_seed`, `schema.rs` fixture re-blessed, `rules.valid_triggers`/`binding_editor` drop the option for a Macro, CONTEXT.md Trigger-mode "except…" clause, no ADR]. **All thirteen tickets resolved — map archived (again) 2026-09-07: ticket 12 spliced the Fire-once keyboard dwell, ticket 13 removed the Additive staging mode (ADR-0009). Destination reached; nothing outstanding.** Both handed-off specs are implemented: the `value=2` repeat in [`kernel-shaped-repeat-impl/`](kernel-shaped-repeat-impl/issues/) (2026-09-06) and the guidance UI in [`output-safety-guidance/`](output-safety-guidance/issues/) (2026-09-07, all four tickets done).
- [Kernel-shaped repeat — implementation](kernel-shaped-repeat-impl/issues/) — **done / archivable** (all 7 tickets resolved 2026-09-06), non-wayfinder (ticket files only, no map). The fresh implementation effort handed off by [`spec-kernel-shaped-repeat.md`](humane-output-rate/spec-kernel-shaped-repeat.md) (humane-output-rate ticket 07). Makes every hold/repeat path whose target is a single keyboard key present as genuine Linux autorepeat (`value=1` → `value=2` at the live `REP_DELAY`→`REP_PERIOD` → `value=0`) instead of `[KeyDown,KeyUp]` pairs. Ticket 07 un-gated [ADR-0008](../docs/adr/0008-physical-plausibility-ceiling-for-synthetic-output.md), restructured the `CONTEXT.md` "Toggle" entry (the "Physical-plausibility ceiling" term already read present-tense), and resolved humane-output-rate ticket 08 (Toggle→Macro regression split landed with ticket 05). Seven tickets: (01) `Injector::repeat_key` primitive — emits `KeyEvent::new(code, 2)`, subject to suppression [prefactor, no blockers]; (02) pure `single_held_key` predicate + `HoldKind` enum + `hold_repeat_kind` classifier [prefactor, no blockers]; (03) thread `&macros` into `trigger::decide` at all 5 call sites + switch to `hold_repeat_kind`, no behaviour change [blocked 02]; (04) Hold-to-repeat single key → `value=2` — surfaces 1/2/3 + single-key deep stage (`TriggerDecision::RepeatKey`, modifier-hold, force-release balance, the queued-RepeatKey-vs-Up ordering test) [blocked 01,03]; (05) Toggle → single key / single-key Macro → sustained autorepeat (`run_toggle_autorepeat` + a startup-resolved `RepeatSchedule` threaded like `toggle_lap_target`; `run_toggle_loop`/`target_lap` survive for multi-step Macro only; also writes humane-output-rate ticket 08's regression split) [blocked 01,03]; (06) Analog-repeat hold-solid → `value=2` at `period_ms` with no initial delay (`RepeatSchedule::advance_fired_steady`, `run_analog_repeat_loop` `HoldSolid` arm) [blocked 01]; (07) un-gate ADR-0008, update `CONTEXT.md`, mark humane-output-rate ticket 08 resolved, README [blocked 04,05,06]. Graph: {01,02} start now → 03 → {04,05}, 06 needs only 01, all three feed 07. Effort decision (Charon, 2026-09-06): single-key deep-stage repeats convert too, for consistency with a physically held key.
- [Post-release development](post-release-development/issues/) — **active**, non-wayfinder (ticket files only, no map). Post-1.0 bug fixes and architectural deepening on the `dev` branch: Daemon/GUI robustness during a D-Bus or Daemon outage (tickets 01–02), and reducing structural friction surfaced by an architecture review — `config::persist_edit` (03), one `config::validate` invariant point (04), the `edit` config-transaction module carved out of `dispatch.rs` (05), one contract-tested `rules` mirror module in the GUI (06), the Chord-detection state machine carved into a pure `chord` module (07), `fire`/`execute_chord_fire` unified into a pure `trigger` module (08), the dispatch task's runtime state concentrated into one `DispatchState` struct (09), the two Depth-fed engines — axis conflict resolution and the Analog-repeat rate curve — carved into `axis` / `analog_repeat` modules (10), the shallow `Command` edit envelope between `dbus` and `edit` collapsed into one `Command::Apply` (11), and the Stepper cursor engine — wrap-around, clamp-on-shrink, drop-on-delete — carved into a pure `stepper` module (12). A second architecture review (2026-09) opened a fresh set: the parallel Macro and Stepper library views in the GUI collapsed into one `LibraryKind`-parameterised module (13). A third review ([`research/architecture-review-2026-09-03.html`](post-release-development/research/architecture-review-2026-09-03.html), 7 candidates) opened one more: the per-Binding half of `config::validate` carved behind a pure `config::binding::check_binding` seam so `schema.rs` stops synthesizing throwaway `Config`s and per-Binding legality becomes a truth table (14). A second pass over that review ([`research/architecture-review-2026-09-03b.html`](post-release-development/research/architecture-review-2026-09-03b.html)) opened two more: the `(firings, toggles)` handle pair — open-coded 2½ times across `DispatchState` / `ChordRuntime` / the charted `stage::Engine` — concentrated into one generic `trigger::Slots<K>` deep module (`slot` firing-wins, `snapshot` toggle-wins, `perform`, `force_release` / `stop_toggle` / `stop_all_toggles`), deleting `ChordRuntime` / `TriggerCtx` / the `trigger_ctx!` macro (**15, done**); and the epoch-guarded disconnect-cleanup protocol behind `SetOutputSuppressed` / `StartDepthStream` — six in-place copies of the same abort-and-supersede race — collapsed into one `dbus::ConnectionScopedTask` with a `supersede`/`finish`/`disarm` surface (16). A fourth review ([`research/architecture-review-2026-09-06.html`](post-release-development/research/architecture-review-2026-09-06.html), 6 candidates, scoped to the just-shipped *Dual-stage keys* work) opened one more: the Quick-Skip / deep-repeat routing in `dispatch::handle_event` folded behind one `stage::Engine::feed` (mirroring `chord::feed`), narrowing the `stage::Engine` interface 10 → 7 methods and dropping `begin_quick_skip` / `is_late` / `primary_handed_off` / `deep_repeat` from the surface — and, per its 2026-09-07 Addendum, moving a dual-stage key's primary press onto the machine-sequenced (dwell-free) firing path, closing `humane-output-rate` ticket 12's redundant-`value=0` residual (**17, done**).
- [Output-safety guidance](output-safety-guidance/issues/) — **done** (all four tickets resolved 2026-09-07), non-wayfinder (ticket files only, no map). The fresh implementation effort handed off by [`spec-user-facing-output-safety-guidance.md`](humane-output-rate/spec-user-facing-output-safety-guidance.md) (humane-output-rate ticket 05). Text-and-placement only — no config validation, no runtime behaviour, no blocking Save. Four tickets: (01) new README `## Output safety` section between Usage & Troubleshooting + the two feature-bullet pointers [no blockers]; (02) the always-visible `⚠️` macro-editor disclaimer **GUI hint** at the top of the Macro editor [no blockers]; (03) the collapsed "About macro safety" `Gtk.Expander` under the disclaimer — two themed tip blocks + a `Full guide: Output safety` link row to the README anchor [blocked 01, 02]; (04) the `⚠️` Analog-repeat selection **GUI hint** below the Trigger-mode dropdown, shown while `analog_repeat` is the live choice, in the binding editor + deep-stage editor [no blockers]. §6 (research → `docs/anti-cheat-input-heuristics.md`) and §7 (`CONTEXT.md` Toast label / GUI hint terms) were done when the spec was gated.
