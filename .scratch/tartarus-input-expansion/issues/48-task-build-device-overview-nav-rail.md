Type: task
Blocked by: 47

## Question

Build [ticket 47's settled nav-rail restructuring](./47-prototype-device-overview-nav-rail.md) (final round-2 design, variant D) for real in `device_overview.py`, against the real Daemon and Tartarus Pro — matching this map's "resolving a ticket means actually building and testing against the real, connected hardware" discipline. This is the chrome restructuring itself; it does not build either of the two features that will eventually populate it (see Scope).

Scope:

- **Profile sidebar**: keep `build_profile_sidebar` exactly as it is today (unchanged — still permanent, still Profile create/rename/delete chrome) but add the round-2 fix ticket 47 found live: an explicit `set_hexpand(False)` on the returned sidebar widget. Without it, GTK4's expand-propagation (inferred from a profile row's `switch_btn`'s own `hexpand=True`, several container levels down) lets the sidebar compete for whatever horizontal slack the *other* destination's content leaves unclaimed — visibly widening it while the Library destination is showing, since Library's content claims less width than the grid's. Ticket 47 measured the pinned width at 197px in both destinations.
- **Replace `build_main_view`'s current assembly** (profile sidebar + grid + `table_revealer`/`table_toggle` + tray mock) with: the (unchanged, pinned) profile sidebar; a plain-text, icon-free "Grid" / "Library" switcher row where the old Action-Table-toggle sat; a `Gtk.Separator` beneath that row; then per-destination content that fully replaces the content area on selection.
- **Grid destination**: the real `build_layer_bar` (Base/Held tabs + Mode-key-role toggle) above the grid, with real spacing between the two — ticket 47's round-2 fix for the grid crowding that row in round 1. Below it, today's real grid+thumbstick assembly (`build_main_view`'s existing device box, unchanged), plus a slot beside the grid, always visible (no toggle), reserved for the Chords list. This ticket does not populate that slot with real content — leave a clearly-labeled placeholder (e.g. a `Gtk.Label` naming [ticket 40](./40-task-build-chord-recording-ux.md)) so the reserved layout space is real and testable, but the actual Chord-recording flow/data is that ticket's job.
- **Library destination**: fully replaces the content area when selected. Same treatment — a clearly-labeled placeholder naming [ticket 41](./41-task-build-stepper-macro-library-ux.md), not real Library content (which needs Daemon/D-Bus support ticket 41 hasn't built yet).
- **Cut Action Table outright, not relocate it**: remove `table_sidebar`/`table_revealer`/`table_toggle` and the `ui_state["table_open"]`/`ui_state["expanded_rows"]` keys from `device_overview.py`; retire `action_table.py` (`build_action_table`/`build_table_row` have no remaining caller) and its tests. Per the user's own real-world verdict during ticket 47's prototyping: unneeded once the real key/mouse-button picker (ticket 42) landed inline in the per-key Binding editor, and actively crowded once it did.
- **Confirm nothing regresses**: Profile create/rename/delete chrome, Base/Held tab switching (including the live `ActiveLayerChanged` auto-follow via `subscribe_layer_changed`), the Mode-key-role toggle, and the per-key Binding editor (`make_input_button`'s modal `Gtk.Window`) all keep working exactly as before — now reached through the Grid destination rather than `build_main_view`'s old single-view assembly. Also confirm the tray mock (`build_tray_mock`) still has a sensible home (it wasn't part of ticket 47's own question, but `build_main_view` currently appends it after the table revealer, which is going away).

Live-hardware verification in scope for this ticket: Profile switching/create/rename/delete against the real Daemon, Base/Held tabs (incl. a real Mode-key hold/release auto-follow), a real Binding edit through the per-key editor, and confirming — live, not just measured in the prototype — that the sidebar's width really doesn't change when switching Grid ↔ Library.

Once this lands, [ticket 40](./40-task-build-chord-recording-ux.md) wires real Chord-recording content into the reserved Chords slot and [ticket 41](./41-task-build-stepper-macro-library-ux.md) wires the real Library screen into the Library destination — both currently `Blocked by` this ticket rather than ticket 47 directly, since the destinations they need don't exist until this build lands.
