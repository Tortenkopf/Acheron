Type: task
Blocked by: 48

## Question

Build the Stepper library and the reusable Macro library for real, against [Design the Stepper list-stepping construct](./03-decide-stepper-list-stepping.md)'s and [Design reusable Macro entities](./15-decide-reusable-macro-entities.md)'s settled shapes, in the GUI form [Prototype the Stepper library and list-editing UX](./31-prototype-stepper-library-ux.md) settled (variant B — two adjacent panels, tab-switched, round 2's rename buttons/reorder buttons/autosave note). No open design questions remain on either the data model or the GUI — this is implementation + live verification, per this map's "resolving a ticket means actually building and testing against the real, connected Tartarus Pro" discipline.

Neither construct exists in the real Daemon/config yet — the Notes' grounding facts and ticket 31's own Answer both confirm `Action::Macro` is still today's inline-only `{ steps }` shape, and `Action::Step`/a Stepper library don't exist at all. This ticket lands both:

- **Config/Daemon**: `Config.macros: HashMap<MacroId, MacroDef>` and a Stepper-list equivalent (global library, per ticket 03/15's settled shapes); `Action::Macro { macro_id }` replacing today's inline `Action::Macro { steps }` (no inline form survives); `Action::Step { stepper: StepperId, direction }`; D-Bus methods for create/rename/delete on both libraries (`DeleteMacro` refusing with `CommandError::InvalidRequest` while still referenced, mirroring `DeleteProfile`; a Stepper list's forward/backward assignment moving it per ticket 03's exclusivity rule); Stepper's per-list runtime cursor stays live-only, never persisted (ticket 03, mirrors `capture_mode`'s precedent).
- **GUI**: the real `Steppers`/`Macros` tab-switched panel pair, ported from `prototype/31-stepper-macro-library-ux`'s variant B almost unchanged — list chrome (name/rename "✎"/delete "×"/"+ New") per panel, item/step editors with ↑/↓/× (Macro's existing inline step editor in `binding_editor.py` relocates here rather than being redesigned), the Stepper assignment row (Forward/Backward Input dropdowns) with the steal-toast, and the "changes save automatically" note in both editor panes. `binding_editor.py`'s Action dropdown gains "Stepper" as a third option alongside Keypress/Macro, assigning a library entry rather than authoring one inline.
- **Item/step key-entry**: reuses whatever real key-entry `binding_editor.py` has at build time (today: [ticket 42](./42-task-build-key-mouse-button-picker-ux.md)'s real `key_picker.py`, live-verified by [ticket 44](./44-task-verify-key-mouse-button-picker-ux-on-hardware.md) — no longer the bare `Gtk.Entry` this ticket's text was written against; ticket 32 has since resolved too) — not a redesign of that control, matching ticket 31's own scope note, just mount the real picker component instead of the bare Entry the note originally fell back to.
- **Mount point**: replaces `build_library_placeholder()` in `device_overview.py`'s Library destination — the plain-text "Grid"/"Library" switcher [ticket 48](./48-task-build-device-overview-nav-rail.md) built and [ticket 49](./49-task-verify-device-overview-nav-rail-on-hardware.md) live-verified. Selecting "Library" already fully swaps the content area (no grid/layer-bar/Chords-slot in sight) and switching back to "Grid" already restores everything, confirmed live — this ticket only replaces the placeholder's content with the real tab-switched panel pair, it doesn't need to build any of the destination-swap chrome itself. Note also: Action Table (the old sidebar this library GUI was once discussed alongside) was cut outright in ticket 48, not relocated — nothing here inherits from or interacts with it.

If the combined scope is too large for one session (precedent: [ticket 18](./18-rework-capture-path-for-analog.md) split into three sequential task tickets when its own build proved too big), split it during this ticket's own resolution rather than pre-splitting here.

## Comments

Split ahead of resolution (via `/to-tickets`, not during a build session) into six tickets covering this ticket's full scope: [51](./51-task-land-macro-library-daemon.md)/[52](./52-task-build-macro-library-gui.md)/[53](./53-task-verify-macro-library-on-hardware.md) for the Macro library (Daemon → GUI → hardware verify) and [54](./54-task-land-stepper-library-daemon.md)/[55](./55-task-build-stepper-library-gui.md)/[56](./56-task-verify-stepper-library-on-hardware.md) for the Stepper library, same shape. Split by construct (Macro vs. Stepper) rather than purely by layer, since a Daemon-only slice isn't independently verifiable without the picker GUI to create/assign a reference — each construct's Daemon ticket still only touches cross-module plumbing, not GUI widgets, per the ticket-21 precedent. Macro goes first since its Action cutover is breaking and its GUI ticket builds the shared tab-switch shell; Stepper's Daemon ticket carries no blocking edge on Macro's (independent schema, purely additive) but its GUI ticket depends on the shell ticket 52 builds. This ticket (41) is left unresolved/unmodified as the parent; work continues on 51 and 54.
