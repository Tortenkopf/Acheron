# 20 — Daemon/device status surface

**What to build:** The GUI reliably tells the user whether the Daemon is running and whether the Tartarus Pro is connected, in both the main window and the tray, and blocks editing when either isn't true — rather than silently letting edits look like they're taking effect when they can't. Build from `prototype/12-daemon-device-status-indicators/prototype.py` directly: it already implements the status chip, tray line, and dimmed disabled-grid overlay against a `DaemonStub` reused from ticket 16's prototype basis — this ticket's job is wiring that to real Daemon-side detection, not redesigning it. See `.scratch/tartarus-keybinder/spec.md` ("Daemon event loop and concurrency" — failure handling, and "GUI information architecture" — status indicators) for the full design.

**Blocked by:** 16

**Status:** resolved

- [x] `CaptureSource` failure handling splits by cause: device-absent (nodes don't exist — at startup, or after a mid-run unplug) is non-fatal and polls the known `/dev/input/by-id/...` paths every ~2s until they reopen cleanly, then resumes; genuine capture errors (e.g. a `uinput` write failure) remain fatal-exit.
- [x] `GetState()` gains a `device_connected: b` field reflecting the poll loop's current view, and a `DeviceConnectionChanged(connected: b)` signal fires on every transition.
- [x] The GUI detects Daemon presence via a live `NameOwnerChanged` watch on `com.acheron.Daemon` on the session bus — not a one-shot check on window open.
- [x] A status chip (colour dot + label) appears above Device Overview and a matching line appears in the tray mock, both reflecting all three reachable states (running+connected / running+disconnected / not running) from the same `GetState()`/signal data, per the prototype.
- [x] Whenever status isn't running+connected, the entire Device Overview grid is disabled (`set_sensitive(False)`) under a dimmed `Gtk.Overlay` with a centered message naming which condition is unmet, matching the prototype's two message strings; the overlay label uses `hexpand=True`/`vexpand=True` alongside `halign`/`valign = CENTER` (the centering pitfall the prototype already caught).
- [x] Live demo: physically unplug the Tartarus Pro — chip, tray line, and grid overlay all flip to disconnected within ~2s, and editing is blocked; replug it and confirm automatic recovery with no Daemon restart. Separately, kill the Daemon process — chip/tray flip to "not running" and editing is blocked. - Ask the user to perform the necessary hardware interactions.
- [x] Automated tests use the fake `CaptureSource` (device-absent scripted) and a fake Daemon D-Bus object to exercise all three status states in the GUI without real hardware or a real Daemon process.

## Comments

Implemented across both processes:

**Daemon (Rust):**
- `daemon/src/capture/evdev_source.rs`: `EvdevCaptureSource` rewritten so each of the three node tasks (`main`/`if01`/`if02`) runs its own open→grab→relay→poll-on-absence loop instead of exiting on a missing/vanished device. `is_device_absent` treats `io::ErrorKind::NotFound` (node doesn't exist yet) and raw `ENODEV` (mid-run unplug on an already-open fd) as the same non-fatal condition, distinct from any other error (still fatal-exit, unchanged from issue 07's original call). A shared `Mutex<[bool; 3]>` combines the three nodes' presence into one `device_connected` view (all three present == connected), pushed into a new `connection_tx: mpsc::Sender<bool>` channel — redundant sends on no actual change are harmless, since the consumer dedupes.
- `daemon/src/capture/mod.rs`/`fake.rs`: `CaptureSource::run` gained the `connection_tx` parameter; `FakeCaptureSource::scripted` (new, alongside the existing `::new`) accepts a `ScriptedEvent::{Physical, Connection}` script for tests that need to interleave device-connection transitions with `PhysicalEvent`s.
- `daemon/src/dispatch.rs`: `run` gained an `rx_connection: mpsc::Receiver<bool>` branch in its `select!` loop, owning a new `device_connected` bool (starts `true`, matching the pre-ticket default) via `handle_connection_change` — same transition-only-emits pattern as `handle_layer_switch`/`ActiveLayerChanged`. `Command::GetState` now returns the real value instead of the hardcoded stub.
- `daemon/src/dbus/mod.rs`: new `DeviceConnectionChanged(connected: b)` signal, emitted directly by the dispatch task (same pattern as `ActiveLayerChanged`).
- `daemon/src/main.rs`: wires the new channel between `EvdevCaptureSource` and `dispatch::run`.
- Tests: `capture::fake::tests::scripted_connection_transitions_are_replayed_in_order_alongside_events`; `dispatch::tests::get_state_reflects_a_reported_device_disconnection`/`redundant_connection_reports_are_idempotent`; `dbus::tests::device_connection_transition_updates_get_state_and_pushes_the_signal_over_real_dbus`. 106 daemon tests green (`cargo test`), `cargo clippy --all-targets` clean.

**GUI (Python):**
- `gui/acheron_gui/daemon_client.py`: `DaemonClient` Protocol/`DBusDaemonClient` gain `subscribe_daemon_running_changed` (via `Gio.bus_watch_name` on `com.acheron.Daemon` — a live watch, not a one-shot check) and `subscribe_device_connection_changed` (wired to the new `DeviceConnectionChanged` signal, same mechanism as `subscribe_layer_changed`).
- `gui/acheron_gui/daemon_stub.py`: `DaemonStub` gains `_daemon_running`/`_device_connected` plus `simulate_daemon_stopped`/`_started` and `simulate_device_disconnected`/`_connected` — the GUI-seam test fake this ticket's checklist calls for. `subscribe_daemon_running_changed` announces the current state immediately on subscribe (mirroring `Gio.bus_watch_name`'s real behavior); `subscribe_device_connection_changed` is signal-only, matching the real Daemon (its initial value instead reaches the GUI through `GetState()`).
- `gui/acheron_gui/device_overview.py`: `STATUS_STATES`/`compute_status`/`PLACEHOLDER_CONFIG`/`build_status_badge`/`build_status_wrapped_view` (new), plus `build_tray_mock`/`build_main_view` both gained an optional `status` parameter to place the tray status line — built from `prototype/12-daemon-device-status-indicators/prototype.py`'s variant C directly (status chip, tray line, dimmed `Gtk.Overlay` with the same two message strings and the `hexpand`/`vexpand` + `halign`/`valign=CENTER` centering fix), per the ticket's "wire it, don't redesign it" instruction.
- `gui/acheron_gui/app.py`: new `_wire_status_tracking` (mirrors `_wire_focus_tracking`'s shape/reentrancy guard) subscribes to both signals and keeps a live `{daemon_running, device_connected}` dict; `rebuild()` only calls `GetConfig()`/`GetState()` while `daemon_running`, keeps the last successfully-fetched Config/profile/layer (or `PLACEHOLDER_CONFIG` if there's never been one) visible-but-inert under the overlay otherwise.
- Tests: `gui/tests/test_app.py` (`_wire_status_tracking` tests, same `DaemonStub`-driven/`_pump_idle_callbacks` pattern as `_wire_focus_tracking`'s) and `gui/tests/test_device_overview.py` (`compute_status`, all three states' overlay/message/badge/tray-line rendering, sensitivity). 67 GUI tests green (`pytest`).

`/code-review` (medium) on the full diff caught three real issues before commit, all fixed:
1. **Initial-render race**: `rebuild()` was called synchronously right after `_wire_status_tracking`/`_wire_focus_tracking` queued their initial state via `GLib.idle_add`, so the GUI could flash "Daemon not running" for a frame on every launch even when already running. Fixed by draining `GLib.MainContext.default()`'s already-queued idle sources once, synchronously, right before that first `rebuild()`.
2. **Non-atomic partial update**: if `GetConfig()` succeeded but the following `GetState()` failed, `last_known["config"]` was updated while `last_known["profile"]`/`["layer"]` stayed stale — a later render could `KeyError` looking up a profile no longer in the newer config. Fixed with `try`/`except`/`else` so both calls' results commit together or not at all.
3. **Fragile structural reach-in**: the tray status line was originally spliced into `build_main_view`'s output via `root.get_last_child()`/`.get_first_child()` (mirroring the prototype's own approach, which had no other option since it reused a `build_main_view` it didn't own). Since this ticket owns the real `build_main_view`/`build_tray_mock` outright, refactored to pass `status` straight through as a parameter instead, removing the coupling to internal append order.

**2026-08-15 (same day) — live-verified against the real Daemon + real GUI + real Tartarus Pro, all three checks passing first try, no code changes needed.**

Launched the real `target/debug/acheron-daemon` and `gui/main.py` against each other and the physical device (already plugged in; ACLs on `/dev/uinput` and `plugdev`-group access to the evdev nodes were already set up from ticket 25's own live testing, so no new permissions work was needed).

- **Baseline**: GUI showed the green "Connected" chip/tray line, grid enabled — confirmed by the user.
- **Unplug**: physically unplugged the Tartarus Pro. GUI flipped to the orange "device disconnected" chip/tray line within ~2s, grid dimmed under the overlay message. Confirmed independently via `busctl --user call com.acheron.Daemon /com/acheron/Daemon com.acheron.Daemon GetState` → `device_connected: false`, and the daemon process (PID unchanged) was still alive — `stderr` showed three benign `evdev`-crate "Failed to ungrab device: No such device (os error 19)" lines (its own `Device` `Drop` impl trying to ungrab a now-vanished fd), not a crash.
- **Replug**: plugged the Tartarus Pro back in. GUI flipped back to green "Connected" within ~2s, grid re-enabled. Confirmed via `GetState` → `device_connected: true`, and the daemon PID was unchanged throughout — no restart, exactly per the ticket's "automatic recovery with no Daemon restart" requirement.
- **Kill Daemon**: sent `SIGKILL` to the daemon process (simulating a crash) while the GUI stayed open. Confirmed the process was dead and `com.acheron.Daemon` no longer owned its bus name (`busctl --user list` showed only `com.acheron.gui` left). GUI flipped to the red "Daemon not running" chip/tray line, grid disabled under the "start it to edit Bindings" overlay — the `NameOwnerChanged`-based presence watch caught the crash live, not just a one-shot check.

No code changes came out of this round — all three scenarios worked as designed on the first attempt.
